## Progress Log

### Task 2: Add parser + append helper in runner (COMPLETED)
- Created `runner/lib/learnings.sh` with four functions:
  - `extract_learnings_block()`: extracts content between `<learnings>` tags using sed
  - `parse_learnings_entry()`: parses `<entry>` fields into global variables (LEARNING_STATUS, LEARNING_DATE, LEARNING_TITLE, LEARNING_WHAT, LEARNING_ROOT_CAUSE, LEARNING_PREVENTION)
  - `append_learning_entry()`: appends normalized Markdown to `.ralphai/LEARNINGS.md`, creating the file with seed header if missing
  - `process_learnings()`: orchestrates extraction, parsing, and appending with appropriate status messages and warnings
- Added `source "$RALPHAI_LIB_DIR/learnings.sh"` to `runner/ralphai.sh` (between prompt.sh and pr.sh)
- Wired `process_learnings "$result"` into the turn loop after agent output capture, before completion check (so learnings are recorded even on the final turn)
- All warnings are non-fatal (return 0) per acceptance criteria
- Bash syntax check passes for both files
- All 150 existing tests pass
- Build and type-check pass

### Remaining tasks:
- Task 3: Wire parser into turn loop — DONE (combined with Task 2)
- Task 4: Update legacy hint in prompt.sh
- Task 5: Add tests for learnings parser/writer
- Task 6: Update docs to match behavior
