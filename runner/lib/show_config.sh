# show_config.sh — --show-config display logic.
# Sourced by ralphai.sh after cli.sh.
#
# The --show-config flag is now handled entirely by cli.sh, which delegates
# to the TypeScript config-cli (src/config-cli.ts -> src/show-config.ts).
# This file is retained as a no-op to avoid changing the ralphai.sh
# source order, which other files may depend on.
